apiVersion: v1
kind: Service
metadata:
  name: hteapot-service
spec:
  type: NodePort
  selector:
    app: hteapot
  ports:
    - protocol: TCP
      port: 8081 # El puerto en el que el servicio estará escuchando internamente
      targetPort: 8081 # El puerto donde la aplicación está corriendo en el Pod
      nodePort: 30081 # El puerto en los nodos para acceder a la aplicación
